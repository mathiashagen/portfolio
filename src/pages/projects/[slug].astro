---
import ProjectPage from "../../components/pages/ProjectPage.astro";
import { getProjectPaths } from "../../lib/projects";

export const getStaticPaths = () => getProjectPaths("nb");

const { project } = Astro.props;
---

<ProjectPage project={project} />
